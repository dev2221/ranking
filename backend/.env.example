PORT=5000
NODE_ENV=development

# Database Configuration (for future PostgreSQL migration)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_password
DB_NAME=internship_ranking

# CORS Configuration
CORS_ORIGIN=http://localhost:3000
